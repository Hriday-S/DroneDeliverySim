# Core Framework
streamlit==1.29.0            # Avoids known GFM regex issue in 1.30.0+

# Core Dependencies
numpy>=1.24
matplotlib>=3.7              # For plotting (Streamlit + pyplot)
rasterio>=1.3                # For GeoTIFF decoding and transform
requests>=2.31               # For HTTP GET to fetch CDL zip files

# Plotting & Graphics (under the hood)
pillow>=9.0                  # Required by matplotlib for image handling
pyparsing>=3.0.9             # Matplotlib dependency
fonttools>=4.39              # Required for font rendering in matplotlib
cycler>=0.11                 # Needed for matplotlib color cycling
kiwisolver>=1.4              # Needed for matplotlib figure rendering

# Streamlit Rendering Fixes
markdown-it-py>=3.0.0        # To support future Streamlit markdown rendering
mdit-py-plugins>=0.4.0       # Needed by markdown-it-py

# Async HTTP (for future use or advanced Streamlit plugins)
aiohttp>=3.8

# Rasterio dependencies (fixes “snug” behavior)
click>=8.0
certifi>=2022.12.7
attrs>=21.2.0

# Compression and ZIP handling
tqdm>=4.65.0                 # Optional progress bar for large downloads
